/*
 *
 * HomePage constants
 *
 */

const domain = 'app/HomePage';

export const INIT = `${domain}/INIT`;

export const MOUNT = `${domain}/MOUNT`;

export const UNMOUNT = `${domain}/UNMOUNT`;

export const SET_CHANNEL = `${domain}/SET_CHANNEL`;

export const RUN_CODE = `${domain}/RUN_CODE`;

export const WRITE_CODE = `${domain}/WRITE_CODE`;

export const WRITE_OUTPUT = `${domain}/WRITE_OUTPUT`;
